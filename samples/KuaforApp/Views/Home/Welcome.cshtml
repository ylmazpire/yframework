@{
    ViewData["Title"] = "Hoş Geldiniz";
}

<div class="text-center py-5">
    <h1 class="mb-3">✂ KuaforApp</h1>
    <p class="text-muted mb-4">Randevu, müşteri ve hizmet yönetimi tek yerde.</p>
    @if (User.Identity?.IsAuthenticated != true)
    {
        <a asp-controller="Account" asp-action="Login" class="btn btn-primary me-2">Giriş Yap</a>
        <a asp-controller="Account" asp-action="Register" class="btn btn-outline-primary">Kayıt Ol</a>
    }
</div>
